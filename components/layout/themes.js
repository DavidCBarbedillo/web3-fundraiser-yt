const light = {
    color: "#000",
    bgColor: '#efe7fd',
    bgImage: 'linear-gradient(to right top, #0000ff, #0006cf, #0007a0, #000674, #00004a)',
    bgDiv: '#fff',
    bgSubDiv: '#efe7fd'
}

const dark = {
    color: '#fff',
    bgColor: '#923cb5',
    bgImage: 'linear-gradient(to right top, #0000ff, #0006cf, #0007a0, #000674, #00004a)',
    bgDiv: 'black',
    bgSubDiv: 'rgb(33, 36, 41)'
}

const themes = {
    light: light,
    dark: dark,
}

export default themes;